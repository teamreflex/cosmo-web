{
  "extends": "@apollo/typescript/tsconfig.bun.json",
  "include": ["src/**/*.ts", "tests/**/*.ts"],
  "exclude": ["node_modules", ".turbo"],

  "compilerOptions": {
    "lib": ["ESNext"],
    "types": ["bun"],
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
